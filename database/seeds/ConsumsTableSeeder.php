<?php

class ConsumsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('consums')->truncate();

		$consums = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('consums')->insert($consums);
	}

}
