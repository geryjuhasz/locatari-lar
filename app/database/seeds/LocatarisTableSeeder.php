<?php

class LocatarisTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('locataris')->truncate();

		$locataris = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('locataris')->insert($locataris);
	}

}
